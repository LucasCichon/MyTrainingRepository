@model Dictionary<string, object>


<div class="bg-primary m-1 p-1 text-white"><h4>Dane klienta</h4></div>

<table class="table table-sm table-bordered m-1 p-1">
    @foreach(var kvp in Model)
    {
        <tr><th>@kvp.Key</th><th>@kvp.Value.ToString()</th></tr>
    }
</table>